import { createContext } from "react";

export const CategoryFilterContext = createContext(null);

export const PriceFilterContext =  createContext(null);

export const CartContext = createContext(null);


